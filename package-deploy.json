{
  "name": "over-hippo-arcade-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Over Hippo Arcade",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network overprotocol",
    "verify": "hardhat verify --network overprotocol"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0"
  }
}